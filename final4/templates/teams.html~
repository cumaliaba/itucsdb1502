{%extends "layout.html"%}

{%block title%}Teams{%endblock%}
{%block content%}
    {%if error%}<p>{{error}}</p>{%endif%}
    <script>
        $(document).ready(function(){
            $('#delete_selectedBtn').click(function(){
                var ids=[];
                $('#table input:checked').each(function(){
                    ids.push(this.name);
                });
                console.log(ids);
                
                $.ajax({
                    url: '/teams',
                    data: {'ids': ids,
                    },
                    type: 'POST',
                    success: function(response) {
                        var data=JSON.parse(response);
                        if (data.status == 'OK') {
                            
                            console.log('DELETED');
                            console.log(data['idlist']);
                            console.log(data.idlist.length);
                            for(var i=0; i<data.idlist.length; i++) {
                                var domname = "tr[name="+data.idlist[i]+"]";
                                console.log(domname);
                                $(domname).hide()
                            }
                        }
                        ids=[];
                    },
                    error: function(error) {
                        console.log(error);
                        ids=[];
                    }
                });

            


            });

            $('.delete_itemBtn').click(function() {
                console.log("deleteItem");
                console.log(this.name);
                $.ajax({
                    url: '/teams',
                    data: {'id': this.name,
                    },
                    type: 'DEL',
                    success: function(response) {
                        var data=JSON.parse(response);
                        if (data.status == 'OK') {
                            
                            console.log('DELETED');
                            console.log(data['idlist']);
                            console.log(data.idlist.length);
                            for(var i=0; i<data.idlist.length; i++) {
                                var domname = "tr[name="+data.idlist[i]+"]";
                                console.log(domname);
                                $(domname).hide()
                            }
                        }
                    },
                    error: function(error) {
                        console.log(error);
                    }
                });

            });
            
            $('.update_itemBtn').click(function() {
                console.log("updateItem");
                console.log(this.name);
                console.log(this);
                var name=this.name;
                $.ajax({
                    url: '/players/'+this.name,
                    data: {'id': this.name,
                    },
                    type: 'GET',
                    success: function(response) {
                        var data=JSON.parse(response);
                        if (data.status == 'OK') {
                            $('#player_item_update').show();
                                                        
                            team=data['team'];
                            $('#team_item_update input[name=name]').val(team['name']);
                            $('#team_item_update input[name=coach_id]').val(team['coach_id']);
                            $('#team_item_update input[name=id]').val(team['id']);
                            var newurl = $('#update_form').attr('action') +'/' +team['id'];
                            $('#update_form').attr('action', newurl);
                        }
                        
                    },
                    error: function(error) {
                        console.log(error);
                    }
                });
            });

        });
    </script>

        
        <table id="table">
            <tr>
                <th></th>
                {%for var in teamtable[t:]%}
                    <th>{{var}}</th>
                {%endfor%}
            </tr>
            {%for t in teams%}
            <tr name="{{t._id}}">
                <td><input type="checkbox" name="{{t._id}}" ></td>
                <td>{{t.name}}</td>
                <td>{{t.country}}</td>
                <td><button name="{{t._id}}" class="delete_itemBtn">Delete</button></td>
                <td><button name="{{t._id}}" class="update_itemBtn">Update</button></td>
            </tr>
            {%endfor%}
        </table>
        <div id="team_item_input" class="team_item_input">
            <form action="{{url_for('team_page')}}" method="post">
            <table id="add_item_table">
                <tr id="add_item_table_tr">
                    {%for var in teamtable[1:] %}
                        <td><input type="text" name="{{var}}" placeholder="{{var}}"></td> 
                    {%endfor%}
                        <td><input type="submit" name="Add" id="add_itemBtn" value="Add"></td>
                </tr>
            </table>
            </form>
        </div>

        <div id="team_item_update" class="hidden">
            <form id="update_form" action="{{url_for('team_page')}}" method="POST">
            <table id="update_item_table">
                <tr id="update_item_table_tr">
                    {%for var in teamtable[1:] %}
                        <td><input type="text" name="{{var}}" placeholder="{{var}}"></td> 
                    {%endfor%}
                        <td><input type="text" name="id" class="hidden"></td>
                        <td><input type="submit" name="Update" id="update" value="Update"></td>
                </tr>
            </table>
            </form>
        </div>


        {%if teams|length>=1 %} 
            <button type="button" id="delete_selectedBtn">Delete Selections</button>
        {%endif%}
{%endblock%}
